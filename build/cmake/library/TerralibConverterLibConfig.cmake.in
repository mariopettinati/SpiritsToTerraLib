# - Config file for the TerralibConverterLib package
# It defines the following variables
#  TERRALIBCONVERTERLIB_INCLUDE_DIRS - include directories for FooBar
#  TERRALIBCONVERTERLIB_LIBRARIES    - libraries to link against
 
# Compute paths
get_filename_component(TERRALIBCONVERTERLIB_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set(TERRALIBCONVERTERLIB_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")
 
# Our library dependencies (contains definitions for IMPORTED targets)
if(NOT TARGET foo AND NOT TerralibConverterLib_BINARY_DIR)
  include("${FOOBAR_CMAKE_DIR}/TerralibConverterLibTargets.cmake")
endif()
 
# These are IMPORTED targets created by TerralibConverterLibTargets.cmake
set(TERRALIBCONVERTERLIB_LIBRARIES foo)